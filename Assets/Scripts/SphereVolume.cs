using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SphereVolume : Volume
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //find the closest point between the box and square by checking the d vector between the center of the sphere and the closest point on the box
    //find nearest vertex or side by checking if sphere center is within any bound
    //check if vector to that point from sphere center is lower magnitude than radius
}
